
export interface ComicPanelData {
  id: string;
  imageUrl: string;
  panelText: string;
  sceneDescription: string;
}

export interface ScriptData {
  sceneDescription: string;
  panelText: string;
}
